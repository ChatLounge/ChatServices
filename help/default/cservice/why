#if soper
Command provided by: chanserv/why
#endif
Help for WHY:

The WHY command shows the access entries an online
user matches.

Syntax: WHY <#channel> [nickname]

Examples:
    /msg &nick& WHY #atheme jilles^
