#if soper
Command provided by: nickserv/set_password
#endif
Help for SET PASSWORD:

SET PASSWORD changes the password of an account.

Syntax: SET PASSWORD <new password>

Example:
    /msg &nick& SET PASSWORD swordfish
