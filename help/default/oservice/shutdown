#if soper
Command provided by: operserv/shutdown
#endif
Help for SHUTDOWN:

SHUTDOWN shuts down services. Services will
not reconnect or restart.

Syntax: SHUTDOWN

Example:
    /msg &nick& SHUTDOWN
